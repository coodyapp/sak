#!/bin/bash
# shellcheck disable=SC2034  # consumed by bin/sak after this is sourced
TOOL_NAME="cloudflared"
TOOL_BIN="cloudflared"
TOOL_DESCRIPTION="Cloudflare's tunneling daemon for exposing local services without inbound firewall rules."
