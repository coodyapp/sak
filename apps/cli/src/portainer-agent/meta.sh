#!/bin/bash
# shellcheck disable=SC2034  # consumed by bin/sak after this is sourced
TOOL_NAME="Portainer Agent"
TOOL_DESCRIPTION="Lightweight agent that lets a remote Portainer server manage this host's Docker environment."
